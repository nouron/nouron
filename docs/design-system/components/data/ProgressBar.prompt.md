Continuous bar for Sol/run progress (Lobby run cards); segmented strip for discrete AP investment (condition-bar / ap-bar embedded at a button's bottom edge).

```jsx
<ProgressBar value={42} max={100} />
<ProgressBar value={3} max={5} segmented segments={5} color="#27ae60" />
```
