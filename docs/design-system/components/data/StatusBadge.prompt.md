Run status (completed/failed) and warning callouts. Never uses the brand red accent for semantics — success/danger/warning are their own palette (design-guide §1: "Akzentfarbe sparsam").

```jsx
<StatusBadge tone="success">Completed</StatusBadge>
<StatusBadge tone="warning">⚠ Bypass active</StatusBadge>
```
