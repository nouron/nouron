Native range input, accent-colored — matches the Hangar Nexus-request "Consul AP" spend slider (continuously-scaling AP cost with no chip, per the design guide's Button rules).

```jsx
<RangeSlider value={consulAp} min={0} max={20} onChange={e => setConsulAp(+e.target.value)} />
```
